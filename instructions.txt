# To open the designer toolkit, use the following command on cmd:
# pyside6-designer

# After you save the .ui file, go to the location and open the cmd.
# Then, type the following command on cmd to convert the .ui file into the python file:
# pyside6-uic test.ui -o test.py
# pyside6-uic name of your ui file.ui -o name of your python file.py


# After the conversion of the .ui file into the .py file, it sometimes comes up with incomplete code.
# In such case, do the following:
# Add this simple version at the end of every .ui file converted to python file:

if __name__ == "__main__":
    import sys
    app = QApplication(sys.argv)
    
    # Create the main window and setup UI
    MainWindow = QMainWindow()
    ui = Ui_MainWindow()
    ui.setupUi(MainWindow)
    
    
    
    MainWindow.show()
    sys.exit(app.exec())

